# VTD runtime fixture

This directory contains a compact, static subset of VTD runtime inputs for unit tests.
The files are representative placeholders for resource roots, object catalogs, configs,
macros, sample scenarios, style assets, tile attributes, addon configuration, and DAT
setup files used by the VTD knowledge-layer baseline.
